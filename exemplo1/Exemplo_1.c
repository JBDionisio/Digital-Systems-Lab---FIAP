void main() {
     //SETUP
     trisd = 0b00000000; //Definindo familia D como saida
     portd = 0b00000000; //Zerando toda a saída D
     
     while(1){
              portd = 0b
              portd = ~portd; //inverte portas
              delay_ms(1000);
     }
}